import { makeStyles, css } from '@/utils/makeStyles';

const useStyles = makeStyles({
  root: ({ colors }) =>  css``,
  main: ({ colors, spacing }) => css``,
  card: ({ borderRadius, padding, spacing }) => css``,
  formContent: css``,
  imgBox: css``,
})

export default useStyles;
